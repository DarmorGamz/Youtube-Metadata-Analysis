import * as React from "react";
import { Card, View} from "@aws-amplify/ui-react";

export function Navbar() {
    return (
        <View>
            <Card>

            </Card>
        </View>
    );
}